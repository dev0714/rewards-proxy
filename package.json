{
  "name": "render-proxy",
  "version": "1.0.0",
  "description": "Proxy service to call target API from a static IP on Render",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "express": "^4.18.2"
  }
}
